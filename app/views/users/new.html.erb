<h1>Sign up Below</h1>

<%= form_for(@user) do |u| %>
   <%= render partial: 'layouts/errors', locals: { object: @user } %>
       <%= u.label :user_name %>  <%= u.text_field :username %>
           <%= u.label :email %>  <%= u.email_field :email %>
           <%= u.label :password %>  <%= u.password_field :password %>

         <%=u.submit "submit" %>

         <%end%>

<%= render 'layouts/homestyle' %>